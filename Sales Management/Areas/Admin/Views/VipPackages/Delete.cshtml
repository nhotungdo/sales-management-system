@model Sales_Management.Models.VipPackage

@{
    ViewData["Title"] = "Xóa gói VIP";
    var color = Model.ColorCode ?? "secondary";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card-modern text-center p-5">
            <div class="mb-4 text-danger">
                <i class="bi bi-exclamation-circle-fill" style="font-size: 4rem;"></i>
            </div>
            
            <h3 class="fw-bold text-dark mb-3">Xóa gói "@Model.Name"?</h3>
            <p class="text-muted mb-4">
                Hành động này không thể hoàn tác. Bạn có chắc chắn muốn xóa gói dịch vụ này khỏi hệ thống không?
            </p>

            <div class="alert alert-light border mb-4 text-start d-inline-block" style="max-width: 400px;">
                <div class="d-flex align-items-center mb-2">
                    <i class="bi bi-tag-fill text-@color me-2"></i>
                    <strong>@Model.Name</strong>
                </div>
                <div class="small text-muted mb-1">Giá: @Model.Price.ToString("N0") VNĐ / @Model.DurationMonth tháng</div>
                <div class="small text-muted">Trạng thái: @Model.Status</div>
            </div>

            <form asp-action="Delete">
                <input type="hidden" asp-for="VipPackageId" />
                <div class="d-flex justify-content-center gap-3">
                    <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Hủy bỏ</a>
                    <button type="submit" class="btn btn-danger rounded-pill px-4">
                        <i class="bi bi-trash me-2"></i> Xác nhận xóa
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
